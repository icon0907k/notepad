```
# 1. Git 초기화 (이미 초기화된 경우 생략)
git init

# 2. 원격 저장소 연결
git remote add origin https://github.com/icon0907k/millionaire_api_server.git

# 3. 변경된 파일 모두 추가
git add .

# 4. 커밋
git commit -m "프로젝트 파일 반영"

# 5. 원격 저장소에 푸시
git push -u origin main

```